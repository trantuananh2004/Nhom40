import React from 'react';

const HeroSection = () => (
  <section style={{ padding: '2rem', textAlign: 'center' }}>
    <h1>Đăng nhập bảo mật với FIDO2</h1>
    <p>Không còn mật khẩu. Trải nghiệm đăng nhập nhanh chóng, an toàn.</p>
  </section>
);

export default HeroSection;
